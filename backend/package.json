{
	"name": "backend",
	"version": "1.0.0",
	"description": "todo app backend",
	"main": "server.js",
	"scripts": {
		"dev": "pm2 start server.js --name \"todo-backend\"  --env production",
		"test": "echo \"Error: no test specified\" && exit 1",
		"stop": "pm2 stop todo-backend"
	},
	"type": "module",
	"author": "pratik bapat",
	"license": "ISC",
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"cors": "^2.8.5",
		"dotenv": "^16.5.0",
		"express": "^5.1.0",
		"express-rate-limit": "^7.5.0",
		"jsonwebtoken": "^9.0.2",
		"mongoose": "^8.15.0"
	}
}
